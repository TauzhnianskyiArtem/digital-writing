spring:
  cache:
    type: redis
  redis:
    host: localhost
    port: 6379
  datasource:
    url: jdbc:postgresql://localhost:5432/digital-write
    username: postgres
    password: 121art121
  jpa:
    generate-ddl: true
    open-in-view: false
    show-sql: true
    properties.hibernate.format_sql: true

server:
  port: 9000